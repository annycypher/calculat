# Отчёт: заливка сайта на sweb (12.09.2026)

## Что залито
- `deploy.ps1` (режим FTP) → 9 папок + 9 файлов в корне + `.htaccess`
- Итого 4416,2 КБ, 108 файлов
- Корень сайта на сервере: **`/public_html`** (там же лежала заглушка SpaceWeb `index.html` 349 694 Б — перезаписана)
- Доступ: FTP `novidesiru@77.222.61.245:21` (Pure-FTPd)

## Приёмка (проверено с сервера sweb по IP с Host: calc-doc.ru)

| Проверка | Результат |
|---|---|
| Главная `/` | ✅ HTTP 200, 30 956 Б, наш контент (`CalcDocs`, `home.css`), заглушки SpaceWeb нет |
| Страницы-инструменты (8 калькуляторов, `invoice.html`, QR/CSV/PDF-конвертеры) | ✅ 200 |
| `search.html`, `privacy.html`, `404.html` | ✅ 200 |
| CSS/JS/шрифты/иконки (`/styles.css`, `/js/*`, `/fonts/*`, `/icons/*`) | ✅ 200 |
| Локальные библиотеки `/libs/*` (pdf.js, worker, xlsx, docx, QR) | ✅ 200 |
| Несуществующая страница | ✅ 404 (наш `404.html`) |
| `/converters/infographic.html` | ✅ 404 (правило из `_redirects` перенесено) |
| **Сжатие** | ✅ `Content-Encoding: gzip` для HTML, CSS, JS, XML |
| **Кэш-заголовки** | ✅ CSS `max-age=86400`, JS/шрифты `max-age=604800`, HTML `must-revalidate` |
| Заголовки безопасности | ✅ CSP, `X-Content-Type-Options`, `X-Frame-Options` (значит **Apache и `.htaccess` на sweb работают**) |
| **Тест больших файлов (обрезка)** | ✅ `pdf.worker.min.js` — 1 087 212 Б скачано байт-в-байт, SHA256 совпадает; так же `xlsx.full.min.js` (952 КБ), `docx.mjs` (695 КБ), `jspdf` (420 КБ) |

**Главный вывод:** обрезки ответов, из-за которой на Cloudflare появился `js/chunkload.js`
и шрифты были нарезаны по 20 КБ, **на sweb нет**. Можно упрощать (фаза 2, после переключения DNS).

## Временное изменение
В `.htaccess` **временно закомментирован редирект `http -> https`**:
```apache
  # RewriteCond %{HTTPS} !=on
  # RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
```
Причина: сертификат на sweb ещё не выпущен, а сам sweb отдаёт `https -> http` — включённый
редирект дал бы петлю. **Включить обратно сразу после выпуска SSL** (и тогда же перезалить файл).

## Осталось
1. Дождаться обновления DNS-кэшей (реестр уже правильный: `ns1/ns2.spaceweb.ru`, `ns3/ns4.spaceweb.pro`).
2. Повторить выпуск SSL в панели sweb → затем вернуть редирект `http -> https` в `.htaccess`.
3. Через сутки — включить HSTS (строка в `.htaccess` уже подготовлена).
4. Фаза 2 (после переключения DNS): убрать `chunkload.js`, вернуть обычные `<script src>`,
   склеить шрифты в целые файлы, при желании — локальный OCR.
5. Удалить через 1–2 недели зону Cloudflare (пока она жива — это наш откат).
